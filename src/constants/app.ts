export const APP_NAME = 'Cozy Journal';
export const APP_VERSION = '1.0.0';
export const BACKUP_FORMAT_VERSION = 1;

/** AsyncStorage key the whole app snapshot is persisted under. */
export const STORAGE_KEY = 'cozy-journal:snapshot';
